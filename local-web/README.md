# Portal Open file Storage - formerly Tshare

This is portal open file storage sharing module, its path alias so its the directory where server.py is that is served 

Connections are local, sender has to have the file and python installed to run the server, reciever gets the **ip** from the sender after senxer already starts up the server